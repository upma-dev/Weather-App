# Fixes "WebSocket connection to ws://localhost:3000/ws failed" for CRA HMR on some setups.
# Hot reload may still work without it; this only affects the dev websocket, not your API.
WDS_SOCKET_PORT=0
